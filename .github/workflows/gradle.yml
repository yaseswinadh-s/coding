name: Java CI with Gradle

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout source code
      - name: Checkout code
        uses: actions/checkout@v4

      # Set up Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

    

      # Build project
      - name: Build with Gradle
        run: ./gradle build

      # Upload JAR artifact (use v4)
      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: gradle-jar
          path: build/libs/*.jar
